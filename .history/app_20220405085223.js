// These lines ACCEPT input and returns new data USING those values
var profileDataArgs = process.argv.slice(2, process.argv.length);
console.log(profileDataArgs);

// If we type in terminal - node app.js "Carlos" "Sabbah"
// If will return an array like so: ['Carlos', 'Sabbah']
